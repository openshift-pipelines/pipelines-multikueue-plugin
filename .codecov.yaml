coverage:
  status:
    project:
      default:
        target: auto
        threshold: 5%
    patch:
      default: false

ignore:
  - "hack/**"
  - "config/**"
  - "vendor/**"
  - "release/**"
  - "bin/**"